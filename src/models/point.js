import mongoose from 'mongoose'

const { Schema } = mongoose

export default new Schema({
  type: {
    type: String,
    enum: ['Point'],
    required: true,
  },
  coordinates: {
    type: [Number],
    required: true,
  },
})
